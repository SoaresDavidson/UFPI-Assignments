#include <stdio.h>
#include <bits/stdc++.h>

using namespace std;

#define MAX 


int main(){
    int n, m, k; cin >> n >> m >> k;
    vector<int> precos(k);
    vector<vector<pair<int, int>>> g;

    for(auto &i : precos) cin >> i;

    return 0;
}
